"""Internal overview builder modules.

The public command remains ``scripts/build_overview.py``.  This package holds
smaller implementation slices so new code can depend on focused modules instead
of importing the full overview facade.
"""

